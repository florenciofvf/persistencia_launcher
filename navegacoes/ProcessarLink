package plugin_navegacao.br.com.processarLink;

alias br.com.persist.plugins.expressao.biblionativo.Http http;

defun main(prefixo, sufixo) {
	const param = {'url': prefixo + sufixo};
	return http.get(param);
}